package com.example.commerce.model;

/**
 * Enum que define as cores permitidas para os veículos.
 * Conforme especificação, apenas BRANCA, PRATA e PRETA são permitidas.
 */
public enum VehicleColor {
    BRANCA,
    PRATA,
    PRETA
} 